-- Migration scaffold (intentionally incomplete).
-- Goal: normalize messy `orders` table into typed columns + related tables.
--
-- NOTE: Keep this file runnable as a starting point.
-- Candidates will replace this with the real normalization migration.
SELECT 1;
